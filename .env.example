# JWT Configuration
JWT_SECRET=generate_with_openssl_rand_base64_32

# App Configuration
# DEVELOPMENT: Set to http://localhost:2998 (where frontend dev server runs)
# LOCAL PRODUCTION TEST: Set to http://localhost:2999 (where server serves built frontend)
# PRODUCTION: Set to your production domain (e.g., https://TEMPLATE_REPLACEME.bawdyshop.space)
APP_BASE_URL=http://localhost:2998

# Server Port
# DEVELOPMENT: 2999 (API only)
# PRODUCTION: Usually 2999 or 80/443 (serves both API and frontend)
PORT=2999

# Database Configuration
# SQLite database file location (relative to project root)
DATABASE_URL="file:./data/db.db"
